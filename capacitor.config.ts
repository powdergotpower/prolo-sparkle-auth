import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.prolo.sparkleauth',
  appName: 'prolo-sparkle-auth',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
